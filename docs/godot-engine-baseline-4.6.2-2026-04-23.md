# Godot Engine Baseline Update (2026-04-23)

## Task
Update repo markdown docs to explicitly state the game/mod engine baseline as `Godot 4.6.2`.

## Assumptions
- The requested scope is documentation-only.
- Existing `4.x` wording should be made explicit where it defines setup requirements.

## Changes Made
1. Updated `readme.md` Godot badge from `4.x` to `4.6.2`.
2. Updated `readme.md` prerequisites and development setup text to `Godot 4.6.2`.
3. Added an engine baseline section to `agents.md` with `Godot 4.6.2`.
4. Updated `docs/context/agents/01-overview-and-structure.md` to include the same baseline.

## Verification
- Confirmed all targeted files now explicitly mention `Godot 4.6.2`.
- No code or gameplay logic files were modified.

## Outcome
Documentation now has a consistent, explicit engine version baseline for setup and contribution guidance.
